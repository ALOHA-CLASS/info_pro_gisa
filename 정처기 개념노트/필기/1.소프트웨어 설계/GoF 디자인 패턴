생성(Creational) 패턴
객체의 생성과 조합을 캡슐화하여 새로운 인스턴스를 만드는 방법에 관한 패턴입니다. 
- 추상 팩토리(Abstract Factory)
- 빌더(Builder)
- 팩토리 메서드(Factory Method)
- 프로토타입(Prototype)
- 싱글톤(Singleton) 

구조(Structural) 패턴
클래스와 객체를 구성해 더 큰 구조를 만드는 방법에 관한 패턴입니다. 
- 어댑터(Adapter)
- 브리지(Bridge)
- 컴포지트(Composite)
- 데코레이터(Decorator)
- 퍼사드(Facade)
- 플라이웨이트(Flyweight)
- 프록시(Proxy) 등이 있습니다.

행동(Behavioral) 패턴
객체 간의 상호작용과 책임을 분산하는 방법에 관한 패턴입니다. 
- 책임 연쇄(Chain of Responsibility)
- 커맨드(Command)
- 인터프리터(Interpreter)
- 반복자(Iterator)
- 중재자(Mediator)
- 메멘토(Memento)
- 옵저버(Observer)
- 상태(State)
- 전략(Strategy)
- 템플릿 메서드(Template Method)
- 방문자(Visitor)
